import 'package:flutter/material.dart';

class SignPaperTrading extends StatelessWidget {
  const SignPaperTrading({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
