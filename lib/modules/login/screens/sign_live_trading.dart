import 'package:flutter/material.dart';

class SignLiveTrading extends StatelessWidget {
  const SignLiveTrading({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
